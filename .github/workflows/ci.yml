name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2

      - name: Lint (fmt)
        run: cargo fmt --all --check

      - name: Lint (clippy)
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Build
        run: cargo build --all-targets

      - name: Test
        run: cargo test --all-targets

      - name: Build Docker image
        if: hashFiles('Dockerfile') != ''
        run: docker build -t spiffe-helper-rust:ci .

  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install required tools
        run: |
          # Install kubectl
          curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
          chmod +x kubectl
          sudo mv kubectl /usr/local/bin/

          # Install kind
          curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.20.0/kind-linux-amd64
          chmod +x ./kind
          sudo mv ./kind /usr/local/bin/kind

          # Install jq
          sudo apt-get update && sudo apt-get install -y jq

          # Install openssl
          sudo apt-get install -y openssl

          # Verify installations
          kubectl version --client
          kind version
          jq --version
          openssl version

      - name: Verify tools
        run: make tools

      - name: Set up environment
        run: make env-up

      - name: Run smoke-test
        run: make smoke-test

      - name: Collect logs on failure
        if: failure()
        run: |
          export KUBECONFIG=$(pwd)/artifacts/kubeconfig
          echo "=== SPIRE Server Logs ==="
          kubectl logs -n spire-server -l app=spire-server --tail=50 || true
          echo ""
          echo "=== SPIRE Agent Logs ==="
          kubectl logs -n spire-agent -l app=spire-agent --tail=50 || true
          echo ""
          echo "=== Pod Status ==="
          kubectl get pods -A || true

      - name: Cleanup
        if: always()
        run: make env-down

  coverage:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Install jq and bc
        run: sudo apt-get update && sudo apt-get install -y jq bc

      - name: Generate code coverage
        id: coverage
        run: |
          cargo llvm-cov --all-features --workspace --json --output-path coverage.json || true
          cargo llvm-cov --all-features --workspace --summary-only
          
          # Debug: show JSON structure if it exists
          if [ -f coverage.json ]; then
            echo "Coverage JSON structure:"
            jq '.' coverage.json | head -50 || cat coverage.json | head -50
          fi

      - name: Extract coverage metrics
        id: extract
        continue-on-error: true
        run: |
          if [ -f coverage.json ]; then
            # Try different possible JSON structures from cargo-llvm-cov
            # cargo-llvm-cov JSON format: { "summary": { "percent_covered": X, "lines": {...}, "functions": {...} } }
            COVERAGE=$(jq -r '.summary.percent_covered // 0' coverage.json 2>/dev/null || echo "0")
            LINES_COVERED=$(jq -r '.summary.lines.covered // 0' coverage.json 2>/dev/null || echo "0")
            LINES_TOTAL=$(jq -r '.summary.lines.count // 0' coverage.json 2>/dev/null || echo "0")
            FUNCTIONS_COVERED=$(jq -r '.summary.functions.covered // 0' coverage.json 2>/dev/null || echo "0")
            FUNCTIONS_TOTAL=$(jq -r '.summary.functions.count // 0' coverage.json 2>/dev/null || echo "0")
            
            # Handle null values
            if [ "${COVERAGE}" = "null" ]; then COVERAGE="0"; fi
            if [ "${LINES_COVERED}" = "null" ]; then LINES_COVERED="0"; fi
            if [ "${LINES_TOTAL}" = "null" ]; then LINES_TOTAL="0"; fi
            if [ "${FUNCTIONS_COVERED}" = "null" ]; then FUNCTIONS_COVERED="0"; fi
            if [ "${FUNCTIONS_TOTAL}" = "null" ]; then FUNCTIONS_TOTAL="0"; fi
            
            # Fallback: calculate coverage from lines if percent_covered is missing
            if [ "${COVERAGE}" = "0" ] && [ "${LINES_TOTAL}" != "0" ]; then
              COVERAGE=$(echo "scale=2; ${LINES_COVERED} * 100 / ${LINES_TOTAL}" | bc 2>/dev/null || echo "0")
            fi
            
            echo "coverage=${COVERAGE}" >> $GITHUB_OUTPUT
            echo "lines_covered=${LINES_COVERED}" >> $GITHUB_OUTPUT
            echo "lines_total=${LINES_TOTAL}" >> $GITHUB_OUTPUT
            echo "functions_covered=${FUNCTIONS_COVERED}" >> $GITHUB_OUTPUT
            echo "functions_total=${FUNCTIONS_TOTAL}" >> $GITHUB_OUTPUT
          else
            echo "Warning: coverage.json not found, using defaults"
            echo "coverage=0" >> $GITHUB_OUTPUT
            echo "lines_covered=0" >> $GITHUB_OUTPUT
            echo "lines_total=0" >> $GITHUB_OUTPUT
            echo "functions_covered=0" >> $GITHUB_OUTPUT
            echo "functions_total=0" >> $GITHUB_OUTPUT
          fi

      - name: Post coverage comment to PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            try {
              const coverage = parseFloat('${{ steps.extract.outputs.coverage }}') || 0;
              const linesCovered = parseInt('${{ steps.extract.outputs.lines_covered }}') || 0;
              const linesTotal = parseInt('${{ steps.extract.outputs.lines_total }}') || 0;
              const functionsCovered = parseInt('${{ steps.extract.outputs.functions_covered }}') || 0;
              const functionsTotal = parseInt('${{ steps.extract.outputs.functions_total }}') || 0;
              
              const coveragePercent = isNaN(coverage) ? '0.00' : coverage.toFixed(2);
              const linesPercent = linesTotal > 0 ? ((linesCovered / linesTotal) * 100).toFixed(2) : '0.00';
              const functionsPercent = functionsTotal > 0 ? ((functionsCovered / functionsTotal) * 100).toFixed(2) : '0.00';
              
              const comment = `## 📊 Code Coverage Report
              
              | Metric | Coverage |
              |--------|----------|
              | **Overall** | **${coveragePercent}%** |
              | Lines | ${linesCovered}/${linesTotal} (${linesPercent}%) |
              | Functions | ${functionsCovered}/${functionsTotal} (${functionsPercent}%) |
              
              _Generated by [cargo-llvm-cov](https://github.com/taiki-e/cargo-llvm-cov)_`;
              
              // Find existing coverage comment
              const comments = await github.rest.issues.listComments({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
              });
              
              const coverageComment = comments.data.find(comment => 
                comment.user.type === 'Bot' && 
                comment.body.includes('Code Coverage Report')
              );
              
              if (coverageComment) {
                // Update existing comment
                await github.rest.issues.updateComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  comment_id: coverageComment.id,
                  body: comment,
                });
              } else {
                // Create new comment
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.issue.number,
                  body: comment,
                });
              }
            } catch (error) {
              console.error('Error posting coverage comment:', error);
              // Don't fail the job if comment posting fails
            }

